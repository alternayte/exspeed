pub async fn run(
    _client: &crate::cli::client::CliClient,
    _stream: &str,
    _last: Option<usize>,
    _no_follow: bool,
    _subject: Option<&str>,
    _from_beginning: bool,
    _json_output: bool,
) -> anyhow::Result<()> {
    todo!()
}
